/**
 * T'REX Tracking — Backend URL Configuration
 * 
 * Untuk GitHub Pages hosting: ganti URL di bawah dengan URL backend kamu.
 * Backend bisa di-deploy gratis di Render / Railway.
 * 
 * Contoh Render URL:
 *   https://indotrans-tracking.onrender.com/api
 * 
 * Untuk development lokal (Node.js server jalan di localhost):
 *   /api
 */
const TRACKING_CONFIG = {
    // ── Development (localhost) ──
    // API_BASE: '/api',

    // ── Production (Render / Railway) ──
    // Ganti dengan URL backend yang sudah kamu deploy
    API_BASE: 'https://YOUR_BACKEND.onrender.com/api'
};
